# 快速配置环境

本页按执行顺序列出环境配置命令。每段命令后只有必要提示；完整版本约束与验收边界见 [docs/REPRODUCIBILITY.md](./docs/REPRODUCIBILITY.md)。

## 0. 准备条件

需要 Linux、NVIDIA 驱动、Conda、Git、Git LFS、`ffmpeg`、`ffprobe`，以及足够磁盘空间。不要向 Conda `base` 安装本项目依赖。

先确认基础工具：

```bash
conda --version
git --version
git lfs version
ffmpeg -version | head -1
ffprobe -version | head -1
nvidia-smi
```

## 1. 设置目录并克隆主仓库

```bash
export EGOENGINE_ROOT=/path/to/egoengine
export REPO_ROOT="$EGOENGINE_ROOT/video_to_spider"
export SPIDER_ROOT="$EGOENGINE_ROOT/spider"
export CONDA_BASE="$(conda info --base)"

mkdir -p "$EGOENGINE_ROOT"
git clone https://github.com/Cat-blizzard/Egoengine.git "$REPO_ROOT"
cd "$REPO_ROOT"
```

`EGOENGINE_ROOT` 是代码、SPIDER、环境归档和工具的共同父目录。

## 2. 克隆固定版本的第三方源码

```bash
mkdir -p "$REPO_ROOT/third_party"

git clone https://github.com/facebookresearch/sam3.git "$REPO_ROOT/third_party/sam3"
git -C "$REPO_ROOT/third_party/sam3" checkout --detach 6dbb02bd38288df755dfa1378000a861e65b84f6

git clone https://github.com/facebookresearch/sam-3d-objects.git "$REPO_ROOT/third_party/sam-3d-objects"
git -C "$REPO_ROOT/third_party/sam-3d-objects" checkout --detach f91db411c50efee93d8db7aeb323885650f6f722

git clone https://github.com/NVlabs/FoundationPose.git "$REPO_ROOT/third_party/FoundationPose"
git -C "$REPO_ROOT/third_party/FoundationPose" checkout --detach a1b694b83e633c2cb6115b9063d940a687759392

git clone https://github.com/rolpotamias/WiLoR.git "$REPO_ROOT/third_party/WiLoR"
git -C "$REPO_ROOT/third_party/WiLoR" checkout --detach fcb911312a38fa8badd30d9656a167485d61b8f9

git clone https://github.com/DepthAnything/Depth-Anything-V2.git "$REPO_ROOT/third_party/Depth-Anything-V2"
git -C "$REPO_ROOT/third_party/Depth-Anything-V2" checkout --detach a561b849ebae10a6f5ef49e26c83cbbcd36c71bf

git clone https://github.com/facebookresearch/spider.git "$SPIDER_ROOT"
git -C "$SPIDER_ROOT" checkout --detach 71238456bf97a7eeb3d0471aa31974e2d404d4ae
git -C "$SPIDER_ROOT" apply "$REPO_ROOT/patches/0001-Make-frozen-SPIDER-lock-reproducible-from-public-PyP.patch"
```

最后一条 patch 将不可访问的内部 Python 包地址替换为公开 PyPI 文件，但不改变包名、版本或哈希。

## 3. 恢复六个 conda-pack 环境

先把下列归档放入 `$EGOENGINE_ROOT/env`：

```text
v2s-core.tar.gz
v2s-depth.tar.gz
v2s-foundationpose.tar.gz
v2s-opt.tar.gz
v2s-sam3d.tar.gz
v2s-wilor.tar.gz
```

每个归档只解压到同名独立环境：

```bash
conda create -y -n v2s-core
tar -xzf "$EGOENGINE_ROOT/env/v2s-core.tar.gz" -C "$CONDA_BASE/envs/v2s-core"
conda run -n v2s-core conda-unpack

conda create -y -n v2s-depth
tar -xzf "$EGOENGINE_ROOT/env/v2s-depth.tar.gz" -C "$CONDA_BASE/envs/v2s-depth"
conda run -n v2s-depth conda-unpack

conda create -y -n v2s-foundationpose
tar -xzf "$EGOENGINE_ROOT/env/v2s-foundationpose.tar.gz" -C "$CONDA_BASE/envs/v2s-foundationpose"
conda run -n v2s-foundationpose conda-unpack

conda create -y -n v2s-opt
tar -xzf "$EGOENGINE_ROOT/env/v2s-opt.tar.gz" -C "$CONDA_BASE/envs/v2s-opt"
conda run -n v2s-opt conda-unpack

conda create -y -n v2s-sam3d
tar -xzf "$EGOENGINE_ROOT/env/v2s-sam3d.tar.gz" -C "$CONDA_BASE/envs/v2s-sam3d"
conda run -n v2s-sam3d conda-unpack

conda create -y -n v2s-wilor
tar -xzf "$EGOENGINE_ROOT/env/v2s-wilor.tar.gz" -C "$CONDA_BASE/envs/v2s-wilor"
conda run -n v2s-wilor conda-unpack
```

如果同名环境已经存在，不要直接覆盖；先备份或换一个全新 Conda 安装前缀。

## 4. 按 SAM3 官方代码创建独立环境

```bash
conda create -y -n v2s-sam3 python=3.12

conda run -n v2s-sam3 python -m pip install \
  torch==2.10.0 torchvision \
  --index-url https://download.pytorch.org/whl/cu128

conda run -n v2s-sam3 python -m pip install -e \
  "$REPO_ROOT/third_party/sam3"
```

SAM3 使用独立 PyTorch/CUDA 栈，不能装进 `v2s-core` 或 base。

## 5. 安装独立 uv 并恢复 SPIDER

```bash
mkdir -p "$EGOENGINE_ROOT/.tools"

conda run -n v2s-core python -m venv \
  "$EGOENGINE_ROOT/.tools/uv"

"$EGOENGINE_ROOT/.tools/uv/bin/python" -m pip install \
  uv==0.12.3

export UV_EXECUTABLE="$EGOENGINE_ROOT/.tools/uv/bin/uv"

cd "$SPIDER_ROOT"
"$UV_EXECUTABLE" sync --frozen
"$UV_EXECUTABLE" lock --check
```

SPIDER 会使用自己的 `.venv`，不会修改 Conda base。

## 6. 放置模型权重

按上游官方方式下载权重；具体官方来源、下载命令和 MANO 文件说明见 [模型权重下载来源.md](./模型权重下载来源.md)。完成后确保以下文件存在：

```bash
test -s "$REPO_ROOT/third_party/sam3/checkpoints/sam3.1_multiplex.pt"
test -s "$REPO_ROOT/third_party/WiLoR/pretrained_models/wilor_final.ckpt"
test -s "$REPO_ROOT/third_party/WiLoR/pretrained_models/model_config.yaml"
test -s "$REPO_ROOT/third_party/WiLoR/pretrained_models/detector.pt"
test -s "$REPO_ROOT/third_party/WiLoR/mano_data/MANO_RIGHT.pkl"
test -s "$REPO_ROOT/third_party/WiLoR/mano_data/mano_mean_params.npz"
test -s "$REPO_ROOT/third_party/Depth-Anything-V2/metric_depth/checkpoints/depth_anything_v2_metric_hypersim_vitl.pth"
test -s "$REPO_ROOT/third_party/sam-3d-objects/checkpoints/hf/pipeline.yaml"
test -s "$REPO_ROOT/third_party/FoundationPose/weights/2024-01-11-20-02-45/model_best.pth"
test -s "$REPO_ROOT/third_party/FoundationPose/weights/2023-10-28-18-33-37/model_best.pth"
```

模型权重体积很大且有各自许可证，因此不提交到 GitHub。缺少任意文件时 preflight 会明确失败。

## 7. 一次性验证全部环境

```bash
cd "$REPO_ROOT"

export GPU=7
export EGOENGINE_ROOT
export UV_EXECUTABLE="$EGOENGINE_ROOT/.tools/uv/bin/uv"

PYTHONNOUSERSITE=1 PYTHONDONTWRITEBYTECODE=1 \
  bash scripts/check_release_preflight.sh

PYTHONNOUSERSITE=1 PYTHONDONTWRITEBYTECODE=1 \
  conda run -n v2s-core \
  python -m pytest -q -p no:cacheprovider
```

预期最后看到：

```text
release preflight passed
157 passed
```

环境验证通过后，直接执行 [quick_start.md](./quick_start.md) 生成最终 MJWP 视频。
