# 模型权重下载来源

本页只记录 EgoEngine 完整流程需要的模型资产、官方来源和目标位置。模型权重不在 Git 仓库或 Conda tar 包中；请接受各上游许可证后自行下载。以下路径均相对于仓库根目录 `REPO_ROOT`。

```bash
export REPO_ROOT=/path/to/Egoengine
```

## 1. SAM 3.1 Object Multiplex

官方来源：[facebook/sam3.1](https://huggingface.co/facebook/sam3.1)；官方代码中的下载映射见 [SAM3 model_builder.py](https://github.com/facebookresearch/sam3/blob/main/sam3/model_builder.py)。该模型可能需要先在 Hugging Face 页面申请访问权限。

```bash
conda activate v2s-sam3
hf auth login
mkdir -p "$REPO_ROOT/third_party/sam3/checkpoints"
hf download facebook/sam3.1 sam3.1_multiplex.pt \
  --local-dir "$REPO_ROOT/third_party/sam3/checkpoints"
test -s "$REPO_ROOT/third_party/sam3/checkpoints/sam3.1_multiplex.pt"
```

## 2. WiLoR 与 MANO

WiLoR 的两个权重链接来自[官方 README](https://github.com/rolpotamias/WiLoR#installation)：

```bash
mkdir -p "$REPO_ROOT/third_party/WiLoR/pretrained_models"
wget -O "$REPO_ROOT/third_party/WiLoR/pretrained_models/detector.pt" \
  https://huggingface.co/spaces/rolpotamias/WiLoR/resolve/main/pretrained_models/detector.pt
wget -O "$REPO_ROOT/third_party/WiLoR/pretrained_models/wilor_final.ckpt" \
  https://huggingface.co/spaces/rolpotamias/WiLoR/resolve/main/pretrained_models/wilor_final.ckpt
```

`pretrained_models/model_config.yaml` 和 `mano_data/mano_mean_params.npz` 随 WiLoR Git 仓库提供，不需要另行下载。`MANO_RIGHT.pkl` 受单独许可证约束：在 [MANO 官方网站](https://mano.is.tue.mpg.de/) 注册并下载 `mano_v*_*.zip`，然后将右手模型放到：

```text
third_party/WiLoR/mano_data/MANO_RIGHT.pkl
```

## 3. Depth Anything V2（室内 Metric Hypersim ViT-L）

官方模型表：[Depth Anything V2 metric depth README](https://github.com/DepthAnything/Depth-Anything-V2/blob/main/metric_depth/README.md)。本流程使用室内场景的 Large/ViT-L 权重：

```bash
mkdir -p "$REPO_ROOT/third_party/Depth-Anything-V2/metric_depth/checkpoints"
wget -O "$REPO_ROOT/third_party/Depth-Anything-V2/metric_depth/checkpoints/depth_anything_v2_metric_hypersim_vitl.pth" \
  'https://huggingface.co/depth-anything/Depth-Anything-V2-Metric-Hypersim-Large/resolve/main/depth_anything_v2_metric_hypersim_vitl.pth?download=true'
```

## 4. SAM 3D Objects

官方来源：[facebook/sam-3d-objects](https://huggingface.co/facebook/sam-3d-objects)；下载步骤见[官方 setup.md](https://github.com/facebookresearch/sam-3d-objects/blob/main/doc/setup.md)。该仓库需要先申请访问权限并登录 Hugging Face。以下命令适用于尚无 `checkpoints/hf` 的全新检出：

```bash
conda activate v2s-sam3d
hf auth login
cd "$REPO_ROOT/third_party/sam-3d-objects"
test ! -e checkpoints/hf
hf download --repo-type model \
  --local-dir checkpoints/hf-download \
  --max-workers 1 \
  facebook/sam-3d-objects
mv checkpoints/hf-download/checkpoints checkpoints/hf
rm -rf checkpoints/hf-download
test -s checkpoints/hf/pipeline.yaml
```

SAM 3D 首次推理还会自动下载并缓存两项依赖：

- MoGe ViT-L：Hugging Face 模型 [`Ruicheng/moge-vitl`](https://huggingface.co/Ruicheng/moge-vitl)，由 `MoGeModel.from_pretrained` 下载。
- DINOv2 ViT-L/14 reg4：官方 [`facebookresearch/dinov2`](https://github.com/facebookresearch/dinov2) 的 `dinov2_vitl14_reg`，由 PyTorch Hub 下载。

它们通常位于 `~/.cache/huggingface/hub/` 和 `~/.cache/torch/hub/`。这两项是离线复现所需的模型缓存，不应当作普通临时缓存删除；Conda tar 包不包含它们。

## 5. FoundationPose

官方说明：[NVlabs/FoundationPose 的 Data prepare](https://github.com/NVlabs/FoundationPose#data-prepare)。从官方 [Google Drive 权重目录](https://drive.google.com/drive/folders/1DFezOAD0oD1BblsXVxqDsl8fj0qzB82i?usp=sharing) 下载并完整保留以下两个目录（包括 `config.yml`）：

```text
third_party/FoundationPose/weights/2023-10-28-18-33-37/  # refiner
third_party/FoundationPose/weights/2024-01-11-20-02-45/  # scorer
```

## 6. 最小文件检查

```bash
test -s "$REPO_ROOT/third_party/sam3/checkpoints/sam3.1_multiplex.pt"
test -s "$REPO_ROOT/third_party/WiLoR/pretrained_models/wilor_final.ckpt"
test -s "$REPO_ROOT/third_party/WiLoR/pretrained_models/detector.pt"
test -s "$REPO_ROOT/third_party/WiLoR/pretrained_models/model_config.yaml"
test -s "$REPO_ROOT/third_party/WiLoR/mano_data/MANO_RIGHT.pkl"
test -s "$REPO_ROOT/third_party/WiLoR/mano_data/mano_mean_params.npz"
test -s "$REPO_ROOT/third_party/Depth-Anything-V2/metric_depth/checkpoints/depth_anything_v2_metric_hypersim_vitl.pth"
test -s "$REPO_ROOT/third_party/sam-3d-objects/checkpoints/hf/pipeline.yaml"
test -s "$REPO_ROOT/third_party/FoundationPose/weights/2023-10-28-18-33-37/model_best.pth"
test -s "$REPO_ROOT/third_party/FoundationPose/weights/2023-10-28-18-33-37/config.yml"
test -s "$REPO_ROOT/third_party/FoundationPose/weights/2024-01-11-20-02-45/model_best.pth"
test -s "$REPO_ROOT/third_party/FoundationPose/weights/2024-01-11-20-02-45/config.yml"
```

不要把 Hugging Face token、登录缓存、模型权重或 MANO 文件提交到公开 Git 仓库。
