"""Paper-faithful EgoEngine reproduction components.

This package is intentionally isolated from ``video_to_spider`` defaults. It
may consume frozen pipeline artifacts, but writes only to a separate experiment
directory.
"""

SCHEMA_VERSION = "1.0"
